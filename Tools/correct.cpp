#include <stdio.h>
#include <queue>
#include <algorithm>
using namespace std;

int main() {
    freopen("../Main/input.txt", "r", stdin);
    freopen("outcorrect.txt", "w", stdout);
    //Escreva aqui qualquer programa que resolva o problema corretamente
    //mesmo sendo ineficiente
    return 0;
}